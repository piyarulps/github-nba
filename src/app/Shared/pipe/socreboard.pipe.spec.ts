import { SocreboardPipe } from './socreboard.pipe';

describe('SocreboardPipe', () => {
  it('create an instance', () => {
    const pipe = new SocreboardPipe();
    expect(pipe).toBeTruthy();
  });
});
